from setuptools import setup, find_packages

setup(
    name="shell-tools",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # зависимости если есть
    ],
    python_requires=">=3.7",
)
